##Spring Configuration
server:
  port: 8080
spring:
  application:
    name: 13-Resiliencia
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:

# TODO: completa estos 3 bloques con los valores exactos del reto
# (3 intentos + backoff exponencial / >50% fallos en ventana de 10 / máx 5 concurrentes).
resilience4j:
  retry:
    instances:
      pasarelaPagos:
        max-attempts: 0        # TODO
        wait-duration: 0ms     # TODO
        enable-exponential-backoff: false  # TODO
  circuitbreaker:
    instances:
      pasarelaPagos:
        failure-rate-threshold: 0   # TODO
        sliding-window-size: 0      # TODO
  bulkhead:
    instances:
      pasarelaPagos:
        max-concurrent-calls: 0     # TODO
