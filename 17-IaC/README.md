# Reto 17 — IaC + escaneo de seguridad

Completa `main.tf` y `pipeline.yml`.

## Pregunta para el assessment

Explica el principio de menor privilegio aplicado al rol IAM que escribiste,
con un ejemplo concreto de un permiso que NO le diste y por qué.
