package pragma.nomina;

/**
 * Solo la implementan los empleados que reciben bono (según el reto,
 * EmpleadoFijo). Ya está completa, no la modifiques.
 */
public interface Bonificable {
    double calcularBono();
}
