package pragma.model;

/**
 * TODO: excepción CHECKED (extiende Exception, no RuntimeException).
 * Debe tener un constructor (String mensaje, Throwable causa) que llame a super(...)
 * para permitir encadenar la causa original.
 */
public class SaldoInsuficienteException extends Exception {
    // TODO: agrega el constructor(String mensaje, Throwable causa)
}
